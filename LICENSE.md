# ライセンス

**未定。いまは無断での再配布・再利用を認めない扱いとする。**

決める前に、次のことを確かめる必要がある。

## 1. `engine/guzen.js` の出どころ

偶然イケメンメーカー(V4.6.4)の `index.html` から、436の宣言を**そのまま切り出した生成物**。
書き直してはいない。**このリポジトリのライセンスは、偶然メーカー側の方針より緩くできない。**

## 2. `engine/sokuseki.js` の出どころ

即席イケメンメーカーの `app.js` からの生成物。同じ扱い。

## 3. `engine/parts.json` / `metrics.json`

即席メーカーの素材から実測した幾何。素材そのものは含まれていないが、
**素材の形を数値として持っている。**

## 4. 語彙

`engine/pools_person.json`(42キー / 856語)は偶然メーカーの `pools` から抜いたもの。
ブランド名・実在の地名・職業名が含まれる。

---

両方のメーカーが自分のものであれば、あとは好きに決めてよい。
公開するなら MIT か Apache-2.0、手元に置くだけならこのままでよい。

決めたら、このファイルを本文に差し替えて `package.json` の `license` も直す。
